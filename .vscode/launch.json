{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run Bot locally",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/bot.js"
        }
    ]
}